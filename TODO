* why is SocketChannel in R?
* UDP

* what happens onEOF? how to tell if remote only closed one half of connection?

* remove dependancy of Thread from Loop => `extends Thread` ?

* SocketChannel and Loop in TCPClient instead of in signature ?
